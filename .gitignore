__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
build/
dist/
_site/
.venv*/

# local dogfood artifacts (regenerated per run; receipts live in docs/)
dogfood_ledger.jsonl
dogfood_cert.json
dogfood_index.json

# unrelated tool artifacts
.freebuff/

# local dev hooks — the guard itself ships (it carries the D21 receipt
# check, erratum D21), only machine-local activation state stays out
scripts/git_hooks/*.local
scripts/git_hooks/setup*

# Lean build cache
proofs/ladder/.lake
proofs/ledger/.lake
proofs/anchor/.lake

# Credential patterns (added to prevent leakage)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519
.netrc
.pypirc
.npmrc
secrets/
.agent-mailbox/
